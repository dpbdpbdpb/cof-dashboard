{
  "name": "cof-dashboard",
  "version": "1.0.0",
  "description": "COF Three-Pillar Governance Dashboard",
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "deploy": "observable build && gh-pages -d dist"
  },
  "dependencies": {
    "@observablehq/framework": "^1.12.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "gh-pages": "^6.1.0"
  }
}
